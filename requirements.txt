# Runtime dependencies — full pinned set, verified working on 2026-07-12
# (Python 3.12, Windows). Install into the venv with:
#     pip install -r requirements.txt
# Offline deploy: pip download -r requirements.txt -d wheels\   (see README)
#
# Direct dependency: faster-whisper. Everything below it is transitive but pinned
# so every install reproduces this exact verified environment.
faster-whisper==1.2.1

# --- inference engine ---
ctranslate2==4.8.1
numpy==2.5.1

# --- VAD (silero via onnxruntime) ---
onnxruntime==1.27.0
flatbuffers==25.12.19
protobuf==7.35.1

# --- audio decoding ---
av==18.0.0

# --- tokenization ---
# (0.22.2, not 0.23.x: transformers 5.x in requirements-train.txt needs <0.23;
#  faster-whisper accepts both — verified working 2026-07-12)
tokenizers==0.22.2

# --- model download / hub client (used by scripts/download_models.py) ---
huggingface_hub==1.23.0
hf-xet==1.5.1
filelock==3.29.7
fsspec==2026.6.0
httpx==0.28.1
httpcore==1.0.9
h11==0.16.0
anyio==4.14.1
certifi==2026.6.17
idna==3.18

# --- misc utilities pulled in by the above ---
click==8.4.2
colorama==0.4.6
packaging==26.2
Pygments==2.20.0
PyYAML==6.0.3
tqdm==4.68.4
typing_extensions==4.16.0
